pipeline {
	agent any 
	
	stages {
	    stage('Checkout') {
	        steps {
		       sh 'cd'
		       sh 'vi newfile'
		      }}
		stage('Build') {
	           steps {
			  sh 'ansible --version'
	                 }}
		
}}

